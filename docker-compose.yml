version: "3.8"
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: genshin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: genshin
    ports:
      - "5433:5432"
    volumes:
      - resinly_db_data:/var/lib/postgresql/data
volumes:
  resinly_db_data: